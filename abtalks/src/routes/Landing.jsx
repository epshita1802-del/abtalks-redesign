// Placeholder — full landing page is built in Step 4.
// Exists now only to prove routing + design tokens are wired correctly.
export default function Landing() {
  return (
    <main className="min-h-screen flex items-center justify-center px-6">
      <div className="text-center">
        <p className="font-mono text-sm text-accent mb-2">/ — landing</p>
        <h1 className="font-display text-2xl font-semibold">ABTalks</h1>
        <p className="text-ink-soft mt-1">Step 2 scaffold — built in Step 4</p>
      </div>
    </main>
  );
}
